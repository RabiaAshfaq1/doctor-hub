# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=doctor_hub_secret_key_2026_final_proj_session
JWT_EXPIRES_IN=7d

# Database Configuration
# Options: mysql, postgres, sqlite
DB_DIALECT=sqlite

# If using mysql or postgres, fill in these credentials:
DB_HOST=localhost
DB_PORT=3306
DB_NAME=doctor_hub
DB_USER=root
DB_PASSWORD=password

# If using sqlite:
DB_STORAGE=./database.sqlite
